name: CloudFormation Update
on:
  push:
    branches:
      - main

jobs:
  cfn_update:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
    - name: Setup AWS credentials
      uses: aws-actions/configure-aws-credentials@v1
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-region: eu-west-1
    - name: Download hash.txt into github workspace
      run: aws s3 cp s3://oldmengrinding-cicd/hash.txt hash.txt
    - name: Make it executable
      run: chmod +x $GITHUB_WORKSPACE/hash.txt
    # - name: Compare hash and update stack
    #   run: cfn_update.sh cloudformation_cicd.yml oldmengrinding




# name: Daniel's tests

# on:
#   push:
#     branches:
#       - main

# jobs:
#   deploy:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout the repo
#         uses: actions/checkout@v3
#       - name: Configure AWS credentials
#         uses: aws-actions/configure-aws-credentials@v1
#       - name: Run the check hash bash script
#         run: ./scripts.sh

#         with:
#           aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
#           aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
#           aws-region: eu-west-1
          
#         - name: List the content in bucket
#           run: aws s3 ls oldmengrinding-cicd/
      # - name: Copy the cloudformation yaml to bucket
      #   run: aws s3 cp cloudformation_cicd.yml s3://oldmengrinding-cicd/cloudformation_cicd.yml
      # - name: update stack
      #   run: aws cloudformation update-stack 
      #         --stack-name oldmengrinding 
      #         --template-url https://oldmengrinding-cicd.s3.eu-west-1.amazonaws.com/cloudformation_cicd.yml
      #         --region eu-west-1      